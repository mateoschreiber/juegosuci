{
  "name": "juegosuci",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run validate && astro build",
    "preview": "astro preview",
    "validate": "node scripts/validate-games.mjs",
    "check": "npm run validate && astro check && astro build"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.13.7",
    "typescript": "^5.9.2"
  }
}
