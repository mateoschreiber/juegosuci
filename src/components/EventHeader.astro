---
import { site } from "../config/site";

interface Props {
  lastUpdated: string;
  editionLabel: string;
  isDemo: boolean;
}

const { lastUpdated, editionLabel, isDemo } = Astro.props;
const updatedLabel = new Intl.DateTimeFormat("es-PY", {
  dateStyle: "short",
  timeStyle: "short",
  timeZone: "America/Asuncion",
}).format(new Date(lastUpdated));
---

<header class="event-header">
  <div class="event-header__topline">
    <a class="brand" href="/" aria-label={`${site.name}, inicio`}>
      <span class="brand__mark" aria-hidden="true">UCI</span>
      <span>{site.name}</span>
    </a>
    <span class="event-header__edition">{editionLabel}</span>
  </div>
  <p class="last-updated">
    <span class="last-updated__dot" aria-hidden="true"></span>
    <span>Actualizado</span>
    <time datetime={lastUpdated}>{updatedLabel}</time>
    {isDemo && <span class="demo-label">Datos de referencia</span>}
  </p>
</header>
